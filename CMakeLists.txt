cmake_minimum_required(VERSION 3.16)

# Project name
set(PROJECT_NAME tomo1firmware)

# Load ESP-IDF's CMake system
include($ENV{IDF_PATH}/tools/cmake/project.cmake)

# Declare the project
project(${PROJECT_NAME})
